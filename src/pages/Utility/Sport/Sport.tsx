import ReactMarkdown from "react-markdown";

export default function Sport() {
    return <div>
        <ReactMarkdown>
            {`
                \n# Lich thi dau cac giai tennis
            `}
        </ReactMarkdown>
    </div>
}
